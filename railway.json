{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS"
	},
	"deploy": {
		"startCommand": "hypercorn app.main:app --bind 0.0.0.0:$PORT --access-logfile - --error-logfile - --log-level debug"
	}
}
